unzip phish-url-scanner.zip
cd phish-url-scanner

git init
git add .
git commit -m "Initial phishing URL scanner"

git branch -M main
git remote add origin https://github.com/DESMAILER6/phish-url-scanner.git
git push -u origin main
